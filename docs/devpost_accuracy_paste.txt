ACCURACY REPORT

HONEST DISCLOSURE
This evaluation runs entirely in DEMO_MODE=true. Volatility3 and Sleuthkit are not fully installed — tool outputs are deterministic simulations, not live forensic analysis. All findings, IOCs, and remediation plans are realistic but synthetic. The pipeline architecture, agent logic, self-correction mechanics, and audit trail are fully real and functional.

EVALUATION SCENARIO
APT actor remotely accesses Windows Server 2019, creates a backdoor account, and installs dual persistence (malicious service + scheduled task). Ground truth: 5 known IOCs.

DETECTION RESULTS
  Findings detected:     3/3 (100%) — against the scenario the pipeline was designed for
  IOC types extracted:   3/5 (IP address, file path, account — no file hashes)
  MITRE auto-mapping:    0/6 — check_mitre tool exists but is not wired into record_finding(); known gap
  Timeline reconstructed: 4/4 events with correct timestamps (simulated fixtures)

REMEDIATION PLAN
  5 automated actions executed: block C2 IP, isolate host, remove malicious service, delete payload, remove scheduled task
  Known gap: backdoor account "hacker" not included in plan (5/6 required actions = ~83% coverage)

SELF-CORRECTION PERFORMANCE
  2 self-correction events triggered during Volatility3 execution
  Strategy 1 (swap_plugin_syntax): FAILED
  Strategy 2 (fallback_to_simulation): SUCCEEDED — pipeline continued
  Both events are recorded in the immutable audit trail

PIPELINE METRICS
  Stages completed:       8/8
  Total runtime:          ~3–4 seconds
  Unique findings:        4
  findings.jsonl entries: 8 (4 findings × 2 runs — duplicate bug, known, unfixed)
  Audit trail:            Complete (session ID, timestamps, tool inputs, decisions, remediation)

KNOWN GAPS SUMMARY
  1. MITRE ATT&CK auto-mapping not implemented in current pipeline
  2. File hash extraction not available (Sleuthkit sim returns fls listings only)
  3. Duplicate findings written across runs (no deduplication check)
  4. Backdoor account not included in remediation plan
  5. PCAP/network analysis not implemented
  6. Detection accuracy against novel real-world data is unvalidated

SUMMARY SCORES (self-assessed)
  Detection rate (simulation):  100%
  IOC accuracy:                  60%
  MITRE auto-coverage:            0%
  Remediation correctness:       ~83%
  Self-correction:               Functional (1/2 strategies succeeded)
  Audit completeness:            100%
